# Blog-Agent
Accepts a blog topic or prompt from the user. Searches the web for new topics or uses provided context. Creates a structured blog outline with section headings. Generates each section in parallel using worker agents. Merges all sections into a cohesive blog. Adds images at relevant locations. Returns a publish-ready blog.
